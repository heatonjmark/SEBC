```
1- set vm.swappiness = 1 in sysctl.conf on all nodes and loaded sysctl -p
2. fs ix xfs nothing to do
3.fs if xfs nothing to do
4. set nofile and nproc for hdfs, mapred and hbase in limits.conf
5. installed bnid-utils and verified nslookup
6. yum installed and enabled nscd
7. installed and started ntpd
```
A

